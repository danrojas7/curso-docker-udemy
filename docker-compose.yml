version: '3'

services:
        web:
                image: 'captacion-php'
                ports:
                        - '8000:8000'
                volumes:
                        - ./:/app
                command: php artisan serve --host=0.0.0.0

        mysql:
                image: 'mysql:5.7'
                ports:
                        - '3306:3306'
                environment:
                        - MYSQL_ROOT_PASSWORD=toor
                volumes:
                        - mysql-data:/var/lib/mysql
        phpmyadmin:
                image: 'phpmyadmin/phpmyadmin'
                ports:
                        - '8080:80'
                mysql_db_server: mysql
                environment:
                        - PMA_ARBITRARY=0
                        - PMA_VERBOSE=Mysql localhost
                        - PMA_HOST=192.168.0.7
                        - PMA_PORT=3306
volumes:
        mysql-data:
